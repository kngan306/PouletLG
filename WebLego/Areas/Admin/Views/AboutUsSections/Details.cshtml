@model WebLego.DataSet.GdrService.AboutUsSection
@{
    ViewData["Title"] = "Chi tiết Nội dung Trang Giới thiệu";
}

<div class="container-fluid py-4">
    <div class="card shadow-sm">
        <div class="card-header bg-light d-flex justify-content-between align-items-center">
            <a asp-action="Index" class="btn btn-outline-secondary btn-sm shadow-sm" aria-label="Quay lại danh sách nội dung">
                <i class="bi bi-arrow-left me-2"></i>Quay lại
            </a>
        </div>
        <div class="card-body">
            <dl class="row g-3 mb-0">
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">ID Nội dung</dt>
                    <dd>@(Model.SectionId)</dd>
                </div>
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">Tiêu đề</dt>
                    <dd>@(Model.Title ?? "-")</dd>
                </div>
                <div class="col-12">
                    <dt class="fw-semibold text-muted">Mô tả</dt>
                    <dd class="text-break">@(Model.Description ?? "-")</dd>
                </div>
                <div class="col-12">
                    <dt class="fw-semibold text-muted">Ảnh</dt>
                    <dd>
                        <img src="@(ViewBag.ImageExists ? Url.Content(Model.ImageUrl) : "/images/placeholder.png")" alt="@(Model.Title ?? "Ảnh nội dung")" class="img-fluid rounded shadow-sm" style="max-width: 400px; max-height: 200px; object-fit: cover;" />
                    </dd>
                </div>
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">Trạng Thái</dt>
                    <dd>
                        <span class="badge @(Model.IsActive ?? false ? "bg-success" : "bg-danger") rounded-pill">
                            @(Model.IsActive ?? false ? "Hoạt động" : "Không hoạt động")
                        </span>
                    </dd>
                </div>
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">Thứ tự</dt>
                    <dd>@(Model.DisplayOrder.ToString() ?? "-")</dd>
                </div>
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">Ngày Tạo</dt>
                    <dd>@(Model.CreatedAt?.ToString("dd/MM/yyyy") ?? "-")</dd>
                </div>
                <div class="col-md-6 col-sm-12">
                    <dt class="fw-semibold text-muted">Người Tạo</dt>
                    <dd>@(Model.CreatedByNavigation?.FullName ?? "-")</dd>
                </div>
            </dl>
        </div>
    </div>
</div>

@section Scripts {
    <style>
        .card {
            border-radius: 0.5rem;
        }

        .btn {
            border-radius: 0.375rem;
        }

        .img-fluid {
            border-radius: 0.25rem;
        }

        .badge {
            font-size: 0.85rem;
            padding: 0.5em 0.75em;
        }

        dt, dd {
            margin-bottom: 0.5rem;
        }

        .text-break {
            word-break: break-word;
        }
    </style>
}