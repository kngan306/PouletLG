@{
    ViewData["Title"] = "Thanh toán thất bại";
    var orderId = TempData["OrderId"] as int?;
}

<style>
    .fail-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 60vh;
        padding-top: 40px;
        text-align: center;
    }

    .btn-retry,
    .btn-secondary {
        padding: 10px 22px;
        border: none;
        border-radius: 10px;
        font-weight: 500;
        text-align: center;
        flex-shrink: 0;
        color: black;
        text-decoration: none;
        margin-top: 15px;
        transition: background-color 0.2s ease;
    }

    .btn-retry {
        background-color: #FECC29;
    }

    .btn-retry:hover {
        background-color: #E0B522;
        color: black;
    }

    .btn-secondary {
        background-color: #d6d6d6;
    }

    .btn-secondary:hover {
        background-color: #bcbcbc;
        color: black;
    }
</style>

<div class="fail-container">
    <h2>Thanh toán thất bại</h2>
    <p>Đã có lỗi xảy ra trong quá trình thanh toán. Vui lòng thử lại.</p>

    @if (orderId.HasValue)
    {
        <a asp-action="RetryPayment" asp-route-orderId="@orderId" class="btn-retry">Thử lại thanh toán</a>
    }

    <a asp-action="Index" asp-controller="Order" class="btn-secondary">Quay lại danh sách đơn hàng</a>
</div>
